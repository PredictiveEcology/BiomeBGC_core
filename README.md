After knitting and rendering `BiomeBGC_core.Rmd`, overwrite this file with a symlink to `BiomeBGC_core.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/docaron/Documents/repos/BiomeBGC_core
rm README.md && ln -s BiomeBGC_core.md README.md
```
